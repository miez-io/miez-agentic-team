# `<short outcome title>`

## User story

As a `<role>`,
I want `<capability>`,
so that `<benefit>`.

**Acceptance criteria**

- [ ] **AC1:** WHEN `<event>` THEN `<observable result>`.
- [ ] **AC2:** WHILE `<state>` THEN `<observable result>`.
- [ ] **AC3:** IF `<failure or edge case>` THEN `<observable result>`.

## Agent task

Implement the user story and satisfy all acceptance criteria above.

**Instructions**

1. `<concrete implementation step>`
2. `<concrete implementation step>`
3. `<add or update the tests that prove the acceptance criteria>`

**Technical requirements**

- `<only the technical constraints needed for this ticket>`
- `<required interfaces, error handling, test data, or selectors>`

**Verification**

- [ ] `<tests that must pass>`
- [ ] `<build, lint, or type checks that must pass>`
